node_modules/
*.log
.DS_Store

# Build/generated output, including private keys. Public material (public JWK,
# JWKS) is copied into evidence/ explicitly; the working out/ dir is never
# committed so a private key can never leak in.
out/
*.pem
private.jwk.json
*.private.jwk.json
secrets/
